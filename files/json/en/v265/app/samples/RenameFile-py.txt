<sample Example>
from native import app

def OnStart():
    oldfile = "test.txt"
    newfile = "test2.txt"
    app.WriteFile(oldfile, "Created: " + str(app.Date()))
    app.RenameFile(oldfile, newfile)
</sample>