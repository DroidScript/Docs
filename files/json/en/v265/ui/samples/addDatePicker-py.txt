<sample Default>
<![CDATA[
class Main extends App:
    def onStart(self):
        # Creates a fullscreen layout with objects vertically centered.
        self.main = ui.addLayout("main", "Linear", "Top,VCenter", 1, 1)
        self.main.setMargins(0.01, 0.01, 0.01, 0.01)

        # Add a button to the main layout
        self.btn = ui.addButton(self.main, "Show Date Picker", "Secondary")
        self.btn.setOnTouch(self.showDatePicker)

        # Initialize date picker
        self.dtp = ui.addDatePicker()

    def showDatePicker(self):
        # Display the date picker dialog
        self.dtp.show()
]]>
</sample>

<sample Default>
<![CDATA[
class Main extends App:
    def onStart(self):
        # Creates a fullscreen layout with objects vertically centered.
        self.main = ui.addLayout("main", "Linear", "Top,VCenter", 1, 1)
        self.main.setMargins(0.01, 0.01, 0.01, 0.01)

        # Add a button to the main layout
        self.btn = ui.addButton(self.main, "Show Date Picker", "Secondary")
        self.btn.setOnTouch(self.showDatePicker)

        # Initialize date picker
        self.dtp = ui.addDatePicker()

    def showDatePicker(self):
        # Display the date picker dialog
        self.dtp.show()
]]>
</sample>

<sample With initial value & portrait>
<![CDATA[
class Main extends App:
    def onStart(self):
        # Creates a fullscreen layout with objects vertically centered.
        self.main = ui.addLayout("main", "Linear", "Top,VCenter", 1, 1)
        self.main.setMargins(0.01, 0.01, 0.01, 0.01)

        # Add a button to the main layout
        self.btn = ui.addButton(self.main, "Show Date Picker", "Secondary")
        self.btn.setOnTouch(self.showDatePicker)

        # With initial value. Date must be of the form "YYYY-MM-DD"
        self.dtp = ui.addDatePicker("2022-12-25", "Portrait")

    def showDatePicker(self):
        # Display the date picker dialog
        self.dtp.show()
]]>
</sample>

<sample With limits & callback>
<![CDATA[
class Main extends App:
    def onStart(self):
        # Creates a fullscreen layout with objects vertically centered.
        self.main = ui.addLayout("main", "Linear", "Top,VCenter", 1, 1)
        self.main.setMargins(0.01, 0.01, 0.01, 0.01)

        # Add a button to the main layout
        self.btn = ui.addButton(self.main, "Show Date Picker", "Secondary")
        self.btn.setOnTouch(self.showDatePicker)

        # With initial value. Date must be of the form "YYYY-MM-DD"
        self.dtp = ui.addDatePicker("2022-12-25", "Portrait")
        self.dtp.setOnSelect(self.onSelect)

    def showDatePicker(self):
        # Display the date picker dialog
        self.dtp.show()

    def onSelect(self, value):
        console.log(value)
]]>
</sample>