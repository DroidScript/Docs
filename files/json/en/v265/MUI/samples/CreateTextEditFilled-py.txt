<sample No Label>
cfg.Light
cfg.MUI

def OnStart():
    from native import app, gfx, ui

    color = gfx.MUI.colors.teal
    app.InitializeUIKit(color.teal)

    lay = ui.CreateLayout("Linear", "VCenter,FillXY")

    tef = ui.CreateTextEditFilled(0.8, "Left", "Type your name")
    lay.AddChild(tef)

    app.AddLayout(lay)
</sample>

<sample With Label>
cfg.Light
cfg.MUI

def OnStart():
    from native import app, gfx, ui

    color = gfx.MUI.colors.teal
    app.InitializeUIKit(color.teal)

    lay = ui.CreateLayout("Linear", "VCenter,FillXY")

    tef = ui.CreateTextEditFilled(0.8, "Left", "Username", True)
    lay.AddChild(tef)

    app.AddLayout(lay)
</sample>

<sample SetOnEnter Callback>
cfg.Dark
cfg.MUI

def OnStart():
    from native import app, gfx, ui

    color = gfx.MUI.colors.teal
    app.InitializeUIKit(color.teal)

    lay = ui.CreateLayout("Linear", "VCenter,FillXY")

    tef = ui.CreateTextEditFilled(0.8, "Left", "Type your name", True)
    tef.SetOnEnter(OnEnter)
    lay.AddChild(tef)

    app.AddLayout(lay)

def OnEnter():
    app.ShowPopup(this.GetText())
</sample>