<sample Basic>
from native import app, cfg
from native import ui as MUI

cfg.Light
cfg.MUI

def OnStart():
    color = MUI.colors.teal
    app.InitializeUIKit(color.teal)

    lay = MUI.CreateLayout("Linear", "FillXY,VCenter")

    list = "Apps, Games, Movies, Books"
    rad = MUI.CreateRadioButtons(list)
    lay.AddChild(rad)

    app.AddLayout(lay)
</sample>

<sample With callback>
from native import app, cfg
from native import ui as MUI

cfg.Light
cfg.MUI

def OnStart():
    color = MUI.colors.teal
    app.InitializeUIKit(color.teal)

    lay = MUI.CreateLayout("Linear", "FillXY,VCenter")

    list = "Apps, Games, Movies, Books"
    rad = MUI.CreateRadioButtons(list, 1, None, "Vertical")
    rad.SetOnTouch(OnTouch)
    lay.AddChild(rad)

    app.AddLayout(lay)

def OnTouch(name, index):
    app.ShowPopup(name + " : " + index)
</sample>