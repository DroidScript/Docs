<sample Unique Text Input>
cfg.Light
cfg.MUI

from native import app

def OnStart():
    color = app.gfx.teal
    app.InitializeUIKit(color.teal)

    lay = app.ui.CreateLayout("Linear", "VCenter,FillXY")

        tef = app.ui.CreateTextEditUnique(0.8, "Left", "Search for something")
        lay.AddChild(tef)

    app.AddLayout(lay)
</sample>