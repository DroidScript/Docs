<sample Basic>
from native import app
cfg.Light
cfg.MUI

def OnStart():
    color = gfx.MUI.colors.teal
    app.InitializeUIKit(color.teal)

    lay = ui.CreateLayout("Linear", "FillXY,VCenter")

    btn = gfx.MUI.CreateButtonRaised("SHOW MODAL", 0.35)
    btn.SetOnTouch(ShowModal)
    lay.AddChild(btn)

    app.AddLayout(lay)

    text = "Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua , sed do eiusmod tempor incididunt ut labore et dolore magna aliqua."
    modal = ui.CreateModal("Modal title", text)

def ShowModal():
    modal.Show()</sample>

<sample Custom Controls>
from native import app
cfg.Light
cfg.MUI

def OnStart():
    color = gfx.MUI.colors.teal
    app.InitializeUIKit(color.teal)

    lay = ui.CreateLayout("Linear", "FillXY,VCenter")

    btn = gfx.MUI.CreateButtonRaised("SHOW MODAL", 0.4)
    btn.SetOnTouch(ShowModal)
    lay.AddChild(btn)

    modal = ui.CreateModal("Modal title", "", "PROCEED", "CANCEL", False)

    modalLay = modal.GetLayout()

    img = app.CreateImage("/Sys/Img/Droid1.png", 0.5)
    modalLay.AddChild(img)

    button = gfx.MUI.CreateButtonRound("SOME BUTTON", 0.4)
    modalLay.AddChild(button)

    app.AddLayout(lay)

def ShowModal():
    modal.Show()</sample>

<sample With Callback>
from native import app
cfg.Dark
cfg.MUI

def OnStart():
    color = gfx.MUI.colors.teal
    app.InitializeUIKit(color.teal)

    lay = ui.CreateLayout("Linear", "FillXY,VCenter")

    btn = gfx.MUI.CreateButtonRaised("SHOW MODAL", 0.35)
    btn.SetOnTouch(ShowModal)
    lay.AddChild(btn)

    app.AddLayout(lay)

    text = "Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua , sed do eiusmod tempor incididunt ut labore et dolore magna aliqua."
    modal = ui.CreateModal("Modal title", text)
    modal.SetOnTouch(OnControlTouch)

def ShowModal():
    modal.Show()

def OnControlTouch(isOk, btnText):
    app.ShowPopup(isOk+" : "+btnText)</sample>

<sample Full Modal>
from native import app
cfg.Light
cfg.MUI

def OnStart():
    lay = ui.CreateLayout("Linear", "FillXY,VCenter")

    btn = gfx.MUI.AddButtonRaised(lay, "SHOW MODAL", 0.35)
    btn.SetOnTouch(ShowModal)

    app.AddLayout(lay)

    text = "Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua , sed do eiusmod tempor incididunt ut labore et dolore magna aliqua."
    modal = ui.CreateModal("Modal title", text, "Save", "Close", False, "Full")
    modal.SetOnTouch(OnControlTouch)

def ShowModal():
    modal.Show()

def OnControlTouch(isOk, btnText):
    app.ShowPopup(isOk+" : "+btnText)</sample>