<sample Basic & Light>
cfg.Light
cfg.MUI

from native import app

def OnStart():
    colors = app.gfx.teal
    app.InitializeUIKit(colors.teal)

    lay = app.ui.CreateLayout("Linear", "VCenter,FillXY")

        div = app.ui.CreateDivider()
        lay.AddChild(div)

    app.AddLayout(lay)

    var text = "You can undone this process."
    dlg = app.ui.CreateDialog("Remove message", text, "REMOVE")
</sample>