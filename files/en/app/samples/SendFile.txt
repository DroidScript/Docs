<sample Send File>
function OnStart()
{
    app.WriteFile( "filex.txt", "Hello World" );
	<b>app.SendFile( app.GetAppPath() + "/filex.txt", "Shared File", app.GetUser() + " shared file.txt with you" );</b>
}
</sample>
