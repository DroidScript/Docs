// ------------- HEADER SECTION -------------


/** # SimulateScroll #
 * Simulates a scroll event on a scrollable app object.
 * $$ app.SimulateScroll(obj, x, y, dx, dy, count, fling) $$ 
 * @param {dso} obj 
 * @param {num_frc} x 
 * @param {num_frc} y 
 * @param {num_frc} dx 
 * @param {num_frc} dy 
 * @param {num} count 
 * @param {num} fling 
*/


